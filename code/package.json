{
  "scripts": {
    "build:web": "cd web && bun run build",
    "dev:web": "cd web && bun run dev"
  },
  "dependencies": {
    "mupdf": "^1.27.0"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.15.0"
  }
}