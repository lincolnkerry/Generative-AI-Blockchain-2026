# Demo Video

**Status:** To be recorded

**Planned contents:**
1. Landing page with EN/KO language toggle
2. Admin dashboard: API key creation, toggle, renew, delete
3. Demo chat: non-sensitive prompt → passes through unchanged
4. Demo chat: PII detected → masked → forwarded to external LLM → response hydrated
5. Demo chat: research secret detected → routed to local LLM
6. MCP tool demo: process tool via Inspector
