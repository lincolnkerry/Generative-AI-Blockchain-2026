A single comprehensive academic-style conceptual architecture diagram for the Privacy Router project. White background, clean professional flat vector style, no 3D, no gradients, no shadows, thin consistent black outlines. Color palette: navy blue (#1a3a5c) for client layer, teal (#2a9d8f) for Privacy Router core, soft coral (#e76f51) for privacy pipeline, steel blue (#4a9eff) for storage layer, purple (#9b59b6) for observability, orange (#e67e22) for external LLMs. 16:9 landscape, generous whitespace, suitable for Korean university term project presentation.

LAYER 1 — TOP: "Client Layer / 클라이언트 계층" (navy blue boxes): Four client boxes labeled "Chat UI (Web)" with browser icon, "Hermes Agent (CLI)" with robot icon, "OpenClaw (Desktop)" with claw icon, "MCP Client" with plug icon. All have downward arrows to the center layer.

LAYER 2 — CENTER: Large teal rounded rectangle labeled "Privacy Router Core / 프라이버시 코어 (FastAPI :8787)". Inside, four endpoint boxes in a row: "/v1/chat/completions (Proxy)", "/v1/responses (OpenResponses)", "/api/v1/classify", "/api/v1/generate", and "MCP Server (stdio)". A shield icon with "Auth (Bearer Token)" in the corner.

LAYER 3 — MIDDLE: Coral rounded rectangle labeled "Privacy Pipeline / 프라이버시 파이프라인". Three stages connected left-to-right with arrows: Stage 1 "Extractor (SLM) / 추출기" with magnifying glass icon, Stage 2 "Router (Rule-based) / 라우터" with decision diamond icon showing three branches (allow, mask_and_send, prompt_user), Stage 3 "Masker (MaskingContract) / 마스커" with mask icon showing "[CATEGORY#N] placeholder" replacement. Below the three branches, small labels: "allow → direct send", "mask_and_send → mask & forward", "prompt_user → 409 confirm".

LAYER 4 — BOTTOM LEFT: Steel blue rounded rectangle labeled "Storage / 저장소 (PostgreSQL)". Five table boxes inside: "providers", "api_keys", "models", "usage_logs", "masking_sessions" connected with relationship lines.

LAYER 5 — BOTTOM CENTER: Orange rounded rectangle labeled "External LLMs / 외부 LLM". Three provider boxes: "OpenRouter", "OpenAI", "Anthropic" each with cloud icons.

LAYER 6 — BOTTOM RIGHT: Purple rounded rectangle labeled "Observability / 관측성". Four connected boxes in flow: "OTel Collector (:4317)" → "Prometheus (:9090)" and "Loki (:3100)" → "Grafana (:3000)" with dashboard icon.

Arrows connecting layers: Clients → Privacy Router Core (downward), Core → Pipeline (downward), Pipeline → External LLMs (rightward), Pipeline → Storage (leftward), Core → Observability (rightward with dotted line labeled "OTLP"). Clean sans-serif font, Korean labels in smaller text below English labels. All text sharp and legible.
