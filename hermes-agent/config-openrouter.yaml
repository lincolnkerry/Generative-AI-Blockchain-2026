# Hermes Agent — OpenRouter + Gemma 4 26b a4b
# Condition 2: OpenRouter direct → Gemma 4 26b a4b (no privacy pipeline)
model:
  provider: custom
  base_url: https://openrouter.ai/api/v1
  api_key: "${OPENROUTER_API_KEY}"
  default: "google/gemma-4-26b-a4b-it"

channels:
  telegram:
    enabled: true
    token: "${TELEGRAM_BOT_TOKEN}"
    dm_policy: open
